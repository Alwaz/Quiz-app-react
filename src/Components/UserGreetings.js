import React from "react";

const UserGreetings = () => {
  // this message will appear only when user complete the quiz.
  return <div>Thankyou for submitting this questionare!</div>;
};

export default UserGreetings;
